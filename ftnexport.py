"""
Message flow from database to links and users
"""

import ftn.msg

def denormalize_message((origdom, origaddr), (destdom, destaddr), msgid, header, body):
  raise Exception("not implemented")
  return #ftn.msg.MSG

